export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME=""


plugins=(
	git
	command-not-found
	fzf
	docker-compose
	# zsh-history-substring-search
	# zsh-syntax-highlighting
	# zsh-autosuggestions

	zsh-autosuggestions
	zsh-syntax-highlighting
	zsh-history-substring-search
	zsh-completions
)

source $ZSH/oh-my-zsh.sh
source ~/.zshprivate

SAVEHIST=100000
HISTFILE=~/.zsh_history

alias cu="chezmoi re-add"
alias bat="bat --style=plain"
alias r="rm -irf"
alias cd="z"
# alias grep="rg"
alias archclean="pacman -Qtdq | sudo pacman -Rns -"
alias "dps"='docker ps -a --format "table {{.ID}}\t{{.Names}}\t{{.Networks}}\t{{.State}}\t{{.Status}}"'
alias "twirforward"='ssh -L 54322:postgres:5432 satont@194.107.126.47 -p 2222'
alias edit="microsoft-edit"
alias dcd="docker context use default"
alias dct="docker context use twir"
alias td="docker --context twir"

alias l='eza -lh --icons=auto --no-permissions' # long list
alias ls='eza -1 --icons=auto --no-permissions' # short list
alias ll='eza -lha --icons=auto --sort=name --group-directories-first --no-permissions' # long list all
alias ld='eza -lhD --icons=auto --no-permissions' # long list dirs
alias lt='eza --icons=auto --tree --no-permissions' # list folder as tree

function tdu() {
	docker --context twir service update --with-registry-auth --force --image registry.twir.app/twirapp/$1 twir_$1
}

function tdl() {
    docker --context twir service logs -f --since 5m twir_$1
}

#alias jq="fx"

reboot_to_windows ()
{
    windows_title=$(grep -i windows /boot/grub/grub.cfg | cut -d "'" -f 2)
    sudo grub-reboot "$windows_title" && sudo reboot
}

export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/gcr/ssh"

# bun completions
# [ -s "/home/satont/.bun/_bun" ] && source "/home/satont/.bun/_bun"

# bun
# export BUN_INSTALL="$HOME/.bun"
# export PATH="$BUN_INSTALL/bin:$PATH"

# export SSH_AUTH_SOCK=~/.bitwarden-ssh-agent.sock

[[ "$TERM_PROGRAM" == "vscode" ]] && . "$(code --locate-shell-integration-path zsh)"

# Created by `pipx` on 2025-12-23 14:30:01
export PATH="$PATH:/home/satont/.local/bin"
export PATH="/home/satont/.bun/bin:$PATH"

# export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"

# opencode
export PATH=/home/satont/.opencode/bin:$PATH
___MY_VMOPTIONS_SHELL_FILE="${HOME}/.jetbrains.vmoptions.sh"; if [ -f "${___MY_VMOPTIONS_SHELL_FILE}" ]; then . "${___MY_VMOPTIONS_SHELL_FILE}"; fi

export PATH=$PATH:/home/satont/.spicetify

# git(){
#   rgit $@
# }

eval "$(mise activate zsh)"
eval "$(zoxide init zsh)"
eval "$(starship init zsh)"
. "$HOME/.cargo/env"

### Added by Zinit's installer
if [[ ! -f $HOME/.local/share/zinit/zinit.git/zinit.zsh ]]; then
    print -P "%F{33} %F{220}Installing %F{33}ZDHARMA-CONTINUUM%F{220} Initiative Plugin Manager (%F{33}zdharma-continuum/zinit%F{220})…%f"
    command mkdir -p "$HOME/.local/share/zinit" && command chmod g-rwX "$HOME/.local/share/zinit"
    command git clone https://github.com/zdharma-continuum/zinit "$HOME/.local/share/zinit/zinit.git" && \
        print -P "%F{33} %F{34}Installation successful.%f%b" || \
        print -P "%F{160} The clone has failed.%f%b"
fi

source "$HOME/.local/share/zinit/zinit.git/zinit.zsh"
autoload -Uz _zinit
(( ${+_comps} )) && _comps[zinit]=_zinit

# zinit light git
# zinit light zdharma/fast-syntax-highlighting  # alternative to zsh-syntax-highlighting
# zinit light zsh-users/zsh-autosuggestions
# zinit light zsh-users/zsh-history-substring-search
# zinit light jethrokuan/fzf
# zinit light docker/compose
# zinit light marlonrichert/zsh-autocomplete
# zinit light rupa/command-not-found


bindkey "^[[1;5C" forward-word  # ctrl + ->
bindkey "^[[1;5D" backward-word # ctrl + <-
bindkey '^[[H' beginning-of-line # replace ctrl + a cursor start of line
bindkey '^[[F' end-of-line # replace ctrl + e cursor end of line


eval "$(/home/satont/.local/bin/mise activate zsh)"
